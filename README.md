# Mirtille-Virtual-Machine
Mirtille is a simple virtual machine 
yes : yes

Raslan = Combination : Rust (for Memory safety) + Erlang (Distributed) + Java (Object) + Ada (Hard RealTime) + Ocaml (Fonctionnal, bitly as Erlang) + YANLA (A new language for Artificial Intelligence)

YANLA = Possibilite to link Object syntaxicly to make a prompt as instruction. Exemple : MyObject connected to (localhost, 5000), if MyObject can do that, then MyObject will be connected with (localhost, 5000) server.